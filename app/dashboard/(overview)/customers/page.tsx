import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Customer',
};

const Page = () => {
  return (
    <div>
        <p>Customer Page</p>
    </div>
  )
};

export default Page;